import assert from 'node:assert/strict';
import {chromium} from '@playwright/test';
const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1280,height:800}});let errors=[];p.on('pageerror',e=>errors.push(e.message));p.on('response',r=>{if(r.status()>=400&&!r.url().includes('fonts'))errors.push(`${r.status()} ${r.url()}`)});
await p.goto('http://batman1989.co.uk/?test=1',{waitUntil:'domcontentloaded',timeout:60000});await p.waitForFunction(()=>window.__batwing?.ready,{},{timeout:60000});assert.match(await p.locator('footer').innerText(),/10.MINUTE MAXIMUM/i);await p.click('#start-ground');await p.waitForFunction(()=>window.__batwing.state.groundReady,{},{timeout:60000});console.log('LIVE',await p.evaluate(()=>({url:location.href,groundReady:window.__batwing.state.groundReady,newCar:!!window.__batwing.ground.art.getObjectByName('Batmobile_body'),wheelCount:window.__batwing.ground.wheels.length})));await p.screenshot({path:'verification/live-domain.png'});console.log({errors});await b.close();if(errors.length)process.exitCode=1;
