import {chromium} from '@playwright/test';
const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1280,height:800}});await p.goto('http://localhost:4173/?test=1');await p.waitForFunction(()=>window.__batwing?.ready);await p.click('#start-ground');await p.waitForFunction(()=>window.__batwing.state.groundReady);await p.click('#drive-launch');for(const [name,x,z,yaw,checkpoint] of [['theatre',-210,0,Math.PI/2,1],['rail',-522.5,-310,0,2]]){await p.evaluate(({x,z,yaw,checkpoint})=>{const g=window.__batwing.ground;g.car.position.set(x,.6,z);g.car.yaw=yaw;g.car.speed=0;g.car.checkpoint=checkpoint;for(let i=0;i<100;i++)g.update(.05,0);g.phase='paused';},{x,z,yaw,checkpoint});await p.screenshot({path:`verification/polish-${name}.png`});await p.evaluate(()=>window.__batwing.ground.phase='play');}await b.close();
