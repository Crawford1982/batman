{
  "name": "batwing-gotham-night",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "build": "vite build --base=./ && node scripts/copy-trailer.mjs",
    "test": "node --test tests/*.test.js",
    "verify": "node tests/browser.mjs",
    "compress": "node scripts/compress-models.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@gltf-transform/core": "^4.5.0",
    "@gltf-transform/functions": "^4.5.0",
    "@playwright/test": "^1.63.0",
    "meshoptimizer": "^1.2.0",
    "prettier": "3.9.6",
    "vite": "^8.2.2",
    "wait-on": "9.1.0"
  }
}
